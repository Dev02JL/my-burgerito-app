module.exports = {
  apps: [{
    name: '{{ pm2_app_name }}',
    script: 'npm',
    args: 'start',
    cwd: '{{ app_directory }}',
    instances: {{ pm2_instances }},
    exec_mode: 'cluster',
    env: {
      NODE_ENV: 'production',
      PORT: {{ env_vars.PORT }}
    },
    max_memory_restart: '{{ pm2_max_memory }}',
    error_file: '{{ app_directory }}/logs/pm2-error.log',
    out_file: '{{ app_directory }}/logs/pm2-out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true,
    autorestart: true,
    watch: false,
    max_restarts: 10,
    min_uptime: '10s'
  }]
};

